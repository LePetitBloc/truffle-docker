version: '3.2'

services:
  ganache:
    image: trufflesuite/ganache-cli
    ports:
      - "7545:7545"

  truffle:
    build:
      context: .
    stdin_open: true
    tty: true
    depends_on:
      - ganache
    volumes:
      - ./truffle:/srv/truffle:rw
